<h1><%= @cocktail.name %></h1>


<% @cocktail.doses.each do |dose| %>
  <p><%= "#{dose.description} of #{dose.ingredient.name}"%>
   <%= link_to '<i class="fas fa-trash-alt"></i>'.html_safe, dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %></p>
<% end %>

<div class="link_to">
  <%= link_to 'Add ingredients', new_cocktail_dose_path(@cocktail) %>
</div>

<!-- <div class="link_to">
<%= link_to "<i class=“fas fa-trash-alt”></i>".html_safe, cocktail_path(@cocktail), method: :delete, data: { confirm: "Are you sure?" } %>
</div> -->

<div class="link_to">
   <%= link_to 'See all cocktails', cocktails_path %>
</div>
